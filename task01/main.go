// Задание 1: Остановить несколько горутин одновременно
//
// context.WithCancel - это "стоп-кран" для горутин.
// Когда ты вызываешь cancel(), все горутины которые слушают этот контекст
// могут это заметить через <-ctx.Done() и аккуратно завершиться.
//
// Это ПРАВИЛЬНЫЙ способ останавливать горутины в Go.
// НЕ глобальный флаг типа var stop bool.
// НЕ закрытие рабочего канала.
// Именно контекст.
//
// Почему? Потому что контекст:
//   - работает через всю цепочку вызовов (можно передавать глубже)
//   - хранит причину отмены (ctx.Err())
//   - поддерживает таймаут из коробки
//   - это стандарт - любая библиотека понимает context.Context
//
// ──────────────────────────────────────────────────────────────────────────────
//
// Напиши функцию worker(ctx context.Context, id int, wg *sync.WaitGroup),
// которая:
//   - вызывает defer wg.Done() в начале
//   - в бесконечном цикле каждые 400мс:
//       * проверяет ctx.Done() - если отменён, выводит
//         "воркер N: получил сигнал остановки, причина: <ctx.Err()>" и выходит
//       * иначе выводит "воркер N: работаю..."
//   - используй select для проверки
//
// В main():
//   1. Создай ctx, cancel := context.WithCancel(context.Background())
//   2. Обязательно вызови defer cancel()
//   3. Создай WaitGroup ЛОКАЛЬНО
//   4. Запусти 3 воркера (передай &wg каждому)
//   5. Подожди 1.2 секунды через time.Sleep
//   6. Вызови cancel()
//   7. Дождись завершения через wg.Wait()
//   8. Выведи "все воркеры остановлены"
//
// Ожидаемый вывод (порядок строк может отличаться):
//   воркер 1: работаю...
//   воркер 2: работаю...
//   воркер 3: работаю...
//   воркер 1: работаю...
//   ... (3 итерации за 1.2 секунды)
//   воркер 3: получил сигнал остановки, причина: context canceled
//   воркер 1: получил сигнал остановки, причина: context canceled
//   воркер 2: получил сигнал остановки, причина: context canceled
//   все воркеры остановлены
//
// Запусти: go run main.go

package main

import (
	"context"
	"fmt"
	"sync"
	"time"
)

// TODO: напиши функцию worker(ctx context.Context, id int, wg *sync.WaitGroup)
// Подсказка:
//   for {
//       select {
//       case <-ctx.Done():
//           // завершиться
//       case <-time.After(400 * time.Millisecond):
//           // работать
//       }
//   }

func main() {
	// TODO: создай контекст с отменой
	// ctx, cancel := context.WithCancel(context.Background())
	// defer cancel()

	// TODO: объяви WaitGroup локально
	var wg sync.WaitGroup

	// TODO: запусти 3 воркера

	// TODO: подожди 1.2 секунды, потом cancel()

	// TODO: wg.Wait() и выведи "все воркеры остановлены"

	_ = context.Background
	_ = fmt.Println
	_ = time.Sleep
	_ = wg // убери когда начнёшь использовать
}
